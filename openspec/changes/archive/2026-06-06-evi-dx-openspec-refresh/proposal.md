## Why

After completing the Jun 2026 WhatsApp series, DX artifacts drifted: stale `openspec-planning.mdc` "Next" table, orphan change folder, Progress.md outdated.

## What Changes

- Remove duplicate `openspec/changes/evi-as-built-baseline-v2/`
- Sync `openspec-planning.mdc` and `Progress.md` with BACKLOG
- Note in BACKLOG that items #2–6 shipped without separate OpenSpec archives

**Out of scope:** New features; Etapa 4 backlog items.

## Impact

Docs and OpenSpec hygiene only.
