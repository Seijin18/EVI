# Proposal: evi-windmill-google-tasks

## Why

Replace `create_task` stub with Google Tasks API via Windmill OAuth resource.

## What Changes

`create_task.py`, `task_tool`, env `WINDMILL_GTASKS_RESOURCE`, spec SCN-TASK-03/05.

## Out of scope

Gmail (separate change).
